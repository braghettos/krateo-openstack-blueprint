# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: swift
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: swift-card-data
    namespace: openstack-system
  widgetData:
    title: Swift
    headerLeft: Provision the Swift blueprint.
    tooltip: Swift blueprint
    icon:
      name: fa-box
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: swift-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-box", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: swift-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: swift-install
        namespace: openstack-system
        resource: buttons
        verb: GET
