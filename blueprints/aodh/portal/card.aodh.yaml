# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aodh
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aodh-card-data
    namespace: openstack-system
  widgetData:
    title: Aodh
    headerLeft: Provision the Aodh blueprint.
    tooltip: Aodh blueprint
    icon:
      name: fa-bell
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: aodh-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-bell", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aodh-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aodh-install
        namespace: openstack-system
        resource: buttons
        verb: GET
