# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: zaqar
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: zaqar-card-data
    namespace: openstack-system
  widgetData:
    title: Zaqar
    headerLeft: Provision the Zaqar blueprint.
    tooltip: Zaqar blueprint
    icon:
      name: fa-comments
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: zaqar-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-comments", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: zaqar-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: zaqar-install
        namespace: openstack-system
        resource: buttons
        verb: GET
