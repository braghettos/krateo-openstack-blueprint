# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: libvirt
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: libvirt-card-data
    namespace: openstack-system
  widgetData:
    title: Libvirt
    headerLeft: Provision the Libvirt blueprint.
    tooltip: Libvirt blueprint
    icon:
      name: fa-server
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: libvirt-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-server", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: libvirt-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: libvirt-install
        namespace: openstack-system
        resource: buttons
        verb: GET
