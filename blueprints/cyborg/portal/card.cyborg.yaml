# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: cyborg
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: cyborg-card-data
    namespace: openstack-system
  widgetData:
    title: Cyborg
    headerLeft: Provision the Cyborg blueprint.
    tooltip: Cyborg blueprint
    icon:
      name: fa-microchip
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: cyborg-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-microchip", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: cyborg-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: cyborg-install
        namespace: openstack-system
        resource: buttons
        verb: GET
