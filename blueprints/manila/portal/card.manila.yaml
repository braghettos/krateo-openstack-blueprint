# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: manila
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: manila-card-data
    namespace: openstack-system
  widgetData:
    title: Manila
    headerLeft: Provision the Manila blueprint.
    tooltip: Manila blueprint
    icon:
      name: fa-folder-tree
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: manila-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-folder-tree", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: manila-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: manila-install
        namespace: openstack-system
        resource: buttons
        verb: GET
