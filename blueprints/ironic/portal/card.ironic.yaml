# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: ironic
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: ironic-card-data
    namespace: openstack-system
  widgetData:
    title: Ironic
    headerLeft: Provision the Ironic blueprint.
    tooltip: Ironic blueprint
    icon:
      name: fa-server
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: ironic-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-server", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: ironic-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: ironic-install
        namespace: openstack-system
        resource: buttons
        verb: GET
