# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: masakari
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: masakari-card-data
    namespace: openstack-system
  widgetData:
    title: Masakari
    headerLeft: Provision the Masakari blueprint.
    tooltip: Masakari blueprint
    icon:
      name: fa-heart-pulse
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: masakari-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-heart-pulse", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: masakari-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: masakari-install
        namespace: openstack-system
        resource: buttons
        verb: GET
