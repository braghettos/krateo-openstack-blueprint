# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: trove
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: trove-card-data
    namespace: openstack-system
  widgetData:
    title: Trove
    headerLeft: Provision the Trove blueprint.
    tooltip: Trove blueprint
    icon:
      name: fa-database
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: trove-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-database", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: trove-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: trove-install
        namespace: openstack-system
        resource: buttons
        verb: GET
