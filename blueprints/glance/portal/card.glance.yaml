# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: glance
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: glance-card-data
    namespace: openstack-system
  widgetData:
    title: Glance
    headerLeft: Provision the Glance blueprint.
    tooltip: Glance blueprint
    icon:
      name: fa-image
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: glance-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-image", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: glance-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: glance-install
        namespace: openstack-system
        resource: buttons
        verb: GET
