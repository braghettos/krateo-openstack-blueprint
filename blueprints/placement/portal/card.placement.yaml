# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: placement
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: placement-card-data
    namespace: openstack-system
  widgetData:
    title: Placement
    headerLeft: Provision the Placement blueprint.
    tooltip: Placement blueprint
    icon:
      name: fa-thumbtack
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: placement-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-thumbtack", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: placement-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: placement-install
        namespace: openstack-system
        resource: buttons
        verb: GET
