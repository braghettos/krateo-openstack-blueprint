# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: freezer
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: freezer-card-data
    namespace: openstack-system
  widgetData:
    title: Freezer
    headerLeft: Provision the Freezer blueprint.
    tooltip: Freezer blueprint
    icon:
      name: fa-box-archive
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: freezer-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-box-archive", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: freezer-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: freezer-install
        namespace: openstack-system
        resource: buttons
        verb: GET
