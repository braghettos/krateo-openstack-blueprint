# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: openstack
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: openstack-card-data
    namespace: openstack-system
  widgetData:
    title: Openstack
    headerLeft: Provision the Openstack blueprint.
    tooltip: Openstack blueprint
    icon:
      name: fa-cloud
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: openstack-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-cloud", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: openstack-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: openstack-install
        namespace: openstack-system
        resource: buttons
        verb: GET
