# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: designate
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: designate-card-data
    namespace: openstack-system
  widgetData:
    title: Designate
    headerLeft: Provision the Designate blueprint.
    tooltip: Designate blueprint
    icon:
      name: fa-globe
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: designate-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-globe", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: designate-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: designate-install
        namespace: openstack-system
        resource: buttons
        verb: GET
