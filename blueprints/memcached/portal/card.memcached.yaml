# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: memcached
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: memcached-card-data
    namespace: openstack-system
  widgetData:
    title: Memcached
    headerLeft: Provision the Memcached blueprint.
    tooltip: Memcached blueprint
    icon:
      name: fa-bolt
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: memcached-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-bolt", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: memcached-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: memcached-install
        namespace: openstack-system
        resource: buttons
        verb: GET
