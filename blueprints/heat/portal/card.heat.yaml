# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: heat
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: heat-card-data
    namespace: openstack-system
  widgetData:
    title: Heat
    headerLeft: Provision the Heat blueprint.
    tooltip: Heat blueprint
    icon:
      name: fa-fire
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: heat-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-fire", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: heat-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: heat-install
        namespace: openstack-system
        resource: buttons
        verb: GET
