# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: gnocchi
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: gnocchi-card-data
    namespace: openstack-system
  widgetData:
    title: Gnocchi
    headerLeft: Provision the Gnocchi blueprint.
    tooltip: Gnocchi blueprint
    icon:
      name: fa-chart-line
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: gnocchi-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-chart-line", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: gnocchi-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: gnocchi-install
        namespace: openstack-system
        resource: buttons
        verb: GET
