# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: skyline
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: skyline-card-data
    namespace: openstack-system
  widgetData:
    title: Skyline
    headerLeft: Provision the Skyline blueprint.
    tooltip: Skyline blueprint
    icon:
      name: fa-gauge-high
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: skyline-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-gauge-high", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: skyline-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: skyline-install
        namespace: openstack-system
        resource: buttons
        verb: GET
