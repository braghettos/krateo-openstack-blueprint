# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: watcher
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: watcher-card-data
    namespace: openstack-system
  widgetData:
    title: Watcher
    headerLeft: Provision the Watcher blueprint.
    tooltip: Watcher blueprint
    icon:
      name: fa-magnifying-glass-chart
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: watcher-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-magnifying-glass-chart", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: watcher-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: watcher-install
        namespace: openstack-system
        resource: buttons
        verb: GET
