# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: mariadb
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: mariadb-card-data
    namespace: openstack-system
  widgetData:
    title: Mariadb
    headerLeft: Provision the Mariadb blueprint.
    tooltip: Mariadb blueprint
    icon:
      name: fa-database
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: mariadb-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-database", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: mariadb-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: mariadb-install
        namespace: openstack-system
        resource: buttons
        verb: GET
