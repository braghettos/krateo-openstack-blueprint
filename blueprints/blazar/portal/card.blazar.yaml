# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: blazar
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: blazar-card-data
    namespace: openstack-system
  widgetData:
    title: Blazar
    headerLeft: Provision the Blazar blueprint.
    tooltip: Blazar blueprint
    icon:
      name: fa-calendar-check
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: blazar-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-calendar-check", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: blazar-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: blazar-install
        namespace: openstack-system
        resource: buttons
        verb: GET
