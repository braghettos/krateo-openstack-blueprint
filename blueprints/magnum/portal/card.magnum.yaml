# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: magnum
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: magnum-card-data
    namespace: openstack-system
  widgetData:
    title: Magnum
    headerLeft: Provision the Magnum blueprint.
    tooltip: Magnum blueprint
    icon:
      name: fa-dharmachakra
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: magnum-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-dharmachakra", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: magnum-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: magnum-install
        namespace: openstack-system
        resource: buttons
        verb: GET
