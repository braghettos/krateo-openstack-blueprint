# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: templates.krateo.io/v1
kind: RESTAction
metadata:
  name: magnum-card-data
  namespace: openstack-system
  annotations:
    krateo.io/verbose: 'true'
spec:
  api:
    - name: cd
      path: /apis/core.krateo.io/v1alpha1/namespaces/openstack-system/compositiondefinitions/magnum
      verb: GET
      continueOnError: true
      errorKey: cdError
  filter: |
    {
      version: (.cd.spec.chart.version // "—"),
      ready: (((.cd.status.conditions // []) | map(select(.type == "Ready"))[0].status) // "Unknown"),
      ts: (.cd.metadata.creationTimestamp // "")
    }
