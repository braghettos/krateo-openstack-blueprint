# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: keystone
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: keystone-card-data
    namespace: openstack-system
  widgetData:
    title: Keystone
    headerLeft: Provision the Keystone blueprint.
    tooltip: Keystone blueprint
    icon:
      name: fa-key
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: keystone-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-key", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: keystone-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: keystone-install
        namespace: openstack-system
        resource: buttons
        verb: GET
