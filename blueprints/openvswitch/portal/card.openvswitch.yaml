# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: openvswitch
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: openvswitch-card-data
    namespace: openstack-system
  widgetData:
    title: Openvswitch
    headerLeft: Provision the Openvswitch blueprint.
    tooltip: Openvswitch blueprint
    icon:
      name: fa-ethernet
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: openvswitch-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-ethernet", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: openvswitch-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: openvswitch-install
        namespace: openstack-system
        resource: buttons
        verb: GET
