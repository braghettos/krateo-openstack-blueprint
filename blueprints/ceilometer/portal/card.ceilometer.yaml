# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: ceilometer
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: ceilometer-card-data
    namespace: openstack-system
  widgetData:
    title: Ceilometer
    headerLeft: Provision the Ceilometer blueprint.
    tooltip: Ceilometer blueprint
    icon:
      name: fa-gauge
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: ceilometer-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-gauge", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: ceilometer-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: ceilometer-install
        namespace: openstack-system
        resource: buttons
        verb: GET
