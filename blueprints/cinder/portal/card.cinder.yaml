# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: cinder
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: cinder-card-data
    namespace: openstack-system
  widgetData:
    title: Cinder
    headerLeft: Provision the Cinder blueprint.
    tooltip: Cinder blueprint
    icon:
      name: fa-hard-drive
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: cinder-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-hard-drive", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: cinder-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: cinder-install
        namespace: openstack-system
        resource: buttons
        verb: GET
