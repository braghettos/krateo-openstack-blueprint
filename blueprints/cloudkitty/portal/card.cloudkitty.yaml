# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: cloudkitty
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: cloudkitty-card-data
    namespace: openstack-system
  widgetData:
    title: Cloudkitty
    headerLeft: Provision the Cloudkitty blueprint.
    tooltip: Cloudkitty blueprint
    icon:
      name: fa-money-bill
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: cloudkitty-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-money-bill", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: cloudkitty-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: cloudkitty-install
        namespace: openstack-system
        resource: buttons
        verb: GET
