# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: tacker
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: tacker-card-data
    namespace: openstack-system
  widgetData:
    title: Tacker
    headerLeft: Provision the Tacker blueprint.
    tooltip: Tacker blueprint
    icon:
      name: fa-tower-cell
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: tacker-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-tower-cell", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: tacker-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: tacker-install
        namespace: openstack-system
        resource: buttons
        verb: GET
