# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: octavia
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: octavia-card-data
    namespace: openstack-system
  widgetData:
    title: Octavia
    headerLeft: Provision the Octavia blueprint.
    tooltip: Octavia blueprint
    icon:
      name: fa-scale-balanced
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: octavia-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-scale-balanced", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: octavia-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: octavia-install
        namespace: openstack-system
        resource: buttons
        verb: GET
