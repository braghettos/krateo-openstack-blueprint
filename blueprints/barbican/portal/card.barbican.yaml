# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: barbican
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: barbican-card-data
    namespace: openstack-system
  widgetData:
    title: Barbican
    headerLeft: Provision the Barbican blueprint.
    tooltip: Barbican blueprint
    icon:
      name: fa-lock
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: barbican-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-lock", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: barbican-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: barbican-install
        namespace: openstack-system
        resource: buttons
        verb: GET
