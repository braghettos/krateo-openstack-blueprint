# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: horizon
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: horizon-card-data
    namespace: openstack-system
  widgetData:
    title: Horizon
    headerLeft: Provision the Horizon blueprint.
    tooltip: Horizon blueprint
    icon:
      name: fa-table-columns
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: horizon-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-table-columns", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: horizon-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: horizon-install
        namespace: openstack-system
        resource: buttons
        verb: GET
