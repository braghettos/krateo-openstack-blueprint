# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: rabbitmq
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: rabbitmq-card-data
    namespace: openstack-system
  widgetData:
    title: Rabbitmq
    headerLeft: Provision the Rabbitmq blueprint.
    tooltip: Rabbitmq blueprint
    icon:
      name: fa-envelope
      color: grey
    tags:
      - iaas
      - openstack
      - private-cloud
    extra: ''
    items: []
    footer:
      - resourceRefId: rabbitmq-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-envelope", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: rabbitmq-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: rabbitmq-install
        namespace: openstack-system
        resource: buttons
        verb: GET
