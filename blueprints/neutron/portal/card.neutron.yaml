# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: neutron
  namespace: openstack-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: neutron-card-data
    namespace: openstack-system
  widgetData:
    title: Neutron
    headerLeft: Provision the Neutron blueprint.
    tooltip: Neutron blueprint
    icon:
      name: fa-network-wired
      color: grey
    tags:
      - openstack-system
    extra: ''
    items: []
    footer:
      - resourceRefId: neutron-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["openstack-system", .version] }
    - forPath: icon
      expression: '${ { name: "fa-network-wired", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: neutron-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: neutron-install
        namespace: openstack-system
        resource: buttons
        verb: GET
